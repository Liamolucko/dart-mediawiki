name: rest_wiki
description: Dart bindings for the Mediawiki REST API, which can also be polyfilled by its traditional Actions API.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  meta: ^1.2.2
  json_annotation: ^3.0.1
  http: ^0.12.2

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  build_runner: 
  json_serializable: ^3.4.0
